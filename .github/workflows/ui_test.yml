name: ui_testing
on:
  schedule:
    # * is a special character in YAML so you have to quote this string
    - cron:  '0,3 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: UI Testing
        run: |
          echo UI testing started